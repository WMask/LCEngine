<UserControl x:Class="common.MainMenu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:common ="clr-namespace:common"
      mc:Ignorable="d" Width="800" Height="600"
      HorizontalAlignment="Left" VerticalAlignment="Top">
    <UserControl.DataContext>
        <common:MainMenuViewModel />
    </UserControl.DataContext>
    <Grid>
        <Rectangle Fill="{StaticResource GreenBrush}" Margin="100">
            <b:Interaction.Behaviors>
                <common:ClickMoveBehavior />
            </b:Interaction.Behaviors>
        </Rectangle>
        <Image Source="{StaticResource Icon1}" Width="180" Height="180" Margin="50" HorizontalAlignment="Left" VerticalAlignment="Top" />
        <TextBlock Text="Widget" Margin="240, 50" HorizontalAlignment="Left" VerticalAlignment="Top" Style="{StaticResource SampleTextBlock}" />
        <Canvas Margin="100">
            <Rectangle x:Name="RedBox" Fill="{StaticResource RedBrush}" Canvas.Left="{Binding PosX}" Canvas.Top="{Binding PosY}" Width="100" Height="100" />
        </Canvas>
    </Grid>
</UserControl>
