<UserControl x:Class="NoesisSample_Blend.MainMenu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:common ="clr-namespace:common"
      mc:Ignorable="d" Width="800" Height="600">
    <UserControl.DataContext>
        <common:MainMenuViewModel />
    </UserControl.DataContext>
    <Grid>
        <Rectangle Fill="{StaticResource GreenBrush}" Margin="100">
            <b:Interaction.Behaviors>
                <common:ClickMoveBehavior />
            </b:Interaction.Behaviors>
        </Rectangle>
        <Canvas Margin="100">
            <Rectangle x:Name="RedBox" Fill="{StaticResource RedBrush}" Canvas.Left="{Binding PosX}" Canvas.Top="{Binding PosY}" Width="100" Height="100" />
        </Canvas>
        <!--Image Source="{StaticResource Icon1}" Width="180" Height="180" Margin="0, 0, 0, 300" /-->
    </Grid>
</UserControl>
